// Logical Operators
// Logical operators are used to combine multiple conditions or expressions and return a boolean value (true or false) based on the evaluation of those conditions. The main logical operators in JavaScript are:

// 1. Logical AND (&&): Returns true if both operands are true, otherwise returns false.
// 2. Logical OR (||): Returns true if at least one of the operands is true, otherwise returns false.
//!3. Logical NOT (!): Returns true if the operand is false, and returns false if the operand is true.

// Example usage of logical operators:
let isAdult = true;
let hasID = false;